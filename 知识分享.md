# Linux内核大致工作流程    

## 1.什么是内核？    
### 内核的概念：  
  - 操作系统内核（Kernel）是指操作系统中常驻内存、运行在最高特权级、负责管理系统资源并提供底层抽象的核心软件组件。内核（Kernel）是操作系统的核心，是计算机硬件和软件之间的桥梁。 它是第一个被加载到内存的程序，负责管理系统的所有资源，直到计算机关机。  
### 内核的任务：
  - 从位置上来说：内核是硬件与软件之间的一个中间层，作用是将应用层序的请求传递给硬件，并充当底层驱动程序，对系统中的各种设备和组件进行寻址。通俗的来说，它像是一个拥有权限的转换器，将用户需求通过内核可以传达给电脑硬件，然后动用权限让其帮用户实现需求。  

  - 从应用程序的层面讲，应用程序与硬件没有联系，只与内核有联系，内核是应用程序知道的层次中的最底层。在实际工作中内核抽象了相关细节。抽象实际上才是最核心的，它是内核作为"转换器"最直接的表现；    
    
  - 从资源管理看：内核是一个资源管理程序。负责将可用的共享资源(CPU时间、磁盘空间、网络连接等)分配得到各个系统进程。  
    
  
    #### 抽象：  
      - 概念：就是隐藏复杂的底层细节，只暴露简单、统一的接口。通俗地来说就是简单化设计，它并不是魔法般地把复杂性变没了，而是把那些你不需要关心的复杂性隐藏起来，只留下你需要的简单接口。      

    - 抽象会主动忽略掉底层那些繁琐的细节。比如你开车时，它隐藏了气缸点火、汽油泵压力、发电机整流这些机械/电子细节。只留下最核心、最通用的交互方式。比如方向盘（控制方向）、油门（加速）、刹车（减速）。  
  
    - 抽象会为这些简化后的接口起一个直观的名字，形成一个新概念。

      在计算机里，它把"一堆存储在磁盘上特定位置的磁信号"变成了"文件"。

      它把"CPU正在运行的一个实例，包含一堆寄存器值和内存地址"变成了"进程"。  
      
      
## 2.内核是如何将用户需求转换的呢？   
### 转换步骤：    
  
    
        应用程序发起请求 (程序调用库函数) ——> glibc包装 ——> 执行syscall指令———>

        cpu切换到内核态 ——>系统调用分发器 (根据系统调用号) ——> 执行具体内核函数——>  

        准备返回值 ——>返回用户态 ——> 库函数处理返回值 ——> 程序继续执行


### 用户态与内核态：  
  - 用户态和内核态是操作系统两种不同的CPU运行模式（也叫特权级别），这是现代操作系统实现保护和隔离的核心机制。  
  - 为什么需要两种状态？  
    想象一下：如果所有程序都能直接访问硬件、修改内存，会发生什么？    

    - 某个 bug 的程序不小心格式化了整个硬盘    

    - 恶意程序直接读取其他进程的内存数据  
    - 程序死循环导致系统完全卡死  
      
  - 为了防止这些问题，CPU设计了特权级别，让操作系统内核运行在最高级别，普通程序运行在最低级别。  
  - 用户态：应用程序运行的地方，受限、安全、隔离
  - 内核态：操作系统运行的地方，全权、危险、核心

### 系统调用：  
  - 系统调用是用户空间程序请求内核服务的唯一合法入口，它本质上属于内核，但他是内核暴露给用户的接口。是用户和内核之间预定义的、受控的接口，用户态进入内核态的安全通道。  
    
    - **为什么需要系统调用这样受控的，安全的入口：** 
       内核拥有最高权限（Ring 0），可以执行任何指令、访问任何内存、操作任何硬件。如果这个“转换器”的入口不受控：   

       [用户程序] --(随便进)--> [内核] --(随便做)--> [硬件]    
         
      会让内核中不同程序相互干扰，导致系统程序崩坏；所以说用户与内核之间需要有一个受控制的接口；  
        
    - 核心作用：    

      1.所有内存操作必须经过内核，内核确保每个程序只能看到自己的内存空间；    

      2.对每个敏感点进行可审计、可控制的安全检查；     

      3.错误被隔离在用户态，内核始终保持稳定。    

      4.所有资源分配都经过内核，内核可以强制执行公平策略。   

      5.通过受控入口提供统一抽象，程序看到的是简单的抽象接口，内核处理复杂的硬件细节。    
      
### syscall指令：    
  
  - syscall 指令，在 x86-64 架构的世界里，就是用户空间程序请求内核服务的那条具体指令。当程序需要读写文件、创建进程或联网时，最终都会通过执行这条指令，将控制权安全地移交给操作系统内核。
  - 工作：  
  当程序执行到 syscall 指令时，CPU 硬件自动完成以下三个动作：    

    - 1. 切换权限级别    

    - 2. 保存返回地址    
    
    - 3. 跳转到内核入口

### 抽象工具：glibc    
  
  - 虽然说系统调用对用户的安全，与系统的程序状态的转换有十分重要的意义，但是若是要直接让用户使用系统调用进行与内核沟通，由于系统调用本质属于内核，会有很多问题：  
    
    - 1 .每个系统调用都要写汇编；   

    - 2 .要记住几百个系统调用号；    

    - 3 .错误处理复杂等多种问题；  
      
  - 所以一个处于系统调用与用户之间的中间层抽象工具，glibc被使用；  
  
     #### glibc:
  - 它是一个为 Linux 应用程序提供标准化接口和基础功能的核心开源库，它是用户程序与 Linux 内核之间的主要桥梁，也是整个操作系统用户态运行的基石。    

  - 简单来说：glioc是介于用户与内核的封包装工具，它将内核中晦涩难懂的汇编代码包装成一个个库函数，将复杂的内核运转过程抽象成可操作的系统接口（就是头文件，库文件等）；
