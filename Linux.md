# Linux系统编程手册学习：  
## 函数栈帧：  
### 基本概念：  
  - 函数栈帧是程序运行时在调用栈上为每个函数调用分配的一块内存区域，用于存储该函数的：

        局部变量

        函数参数

        返回地址

        保存的寄存器值

        临时数据
  

### 具体存储结构：  
          
         高地址
         +-----------------+
         | 调用者的栈帧    |
         +-----------------+
         | 参数n           | ← 被调用函数的参数
         | ...             |
         | 参数2           |
         | 参数1           |
         +-----------------+
         | 返回地址        | ← 调用后返回到哪里
         +-----------------+
         | 旧的ebp         | ← 调用者的帧指针（保存）
         +-----------------+
         | 局部变量1       |
         | 局部变量2       |
         | ...             |
         +-----------------+
         | 临时变量        |
         +-----------------+
         | 保存的寄存器    |
         +-----------------+
         | 对齐填充        |
         +-----------------+
         低地址  
             
### 优点：  
  1 . 自动化的内存管理：    

- 零内存泄漏风险：栈帧销毁时，所有局部变量自动释放

- 无需手动管理：程序员不用写free()

- 生命周期明确：与函数执行周期完全绑定    

2 . 高效的内存分配/释放    

3 . CPU缓存友好性  
  - 对cup缓存性能消耗不大；