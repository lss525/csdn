# 西邮 Linux 兴趣小组 2024 纳新面试题
*学长寄语：长期以来，西邮 Linux 兴趣小组的面试题以难度之高名扬西邮校内。我们作为出题人也清楚的知道这份试题略有难度。请你动手敲一敲代码。别担心，若有同学能完成一半的题目，就已经十分优秀。其次，相比于题目的答案，我们对你的思路和过程更感兴趣，或许你的答案略有瑕疵，但你正确的思路和对知识的理解足以为你赢得绝大多数的分数。最后，做题的过程也是学习和成长的过程，相信本试题对你更加熟悉地掌握 C 语言一定有所帮助。祝你好运。我们东区逸夫楼 FZ103 见！*  

1.本题目只作为西邮 Linux 兴趣小组 2024 纳新面试的有限参考。   
2.为节省版面，本试题的程序源码省去了 #include 指令。  
3.本试题中的程序源码仅用于考察 C 语言基础，不应当作为 C 语言「代码风格」的范例。  
4.所有题目编译并运行于 x86_64 GNU/Linux 环境。  

## 0. 聪明的吗喽
*一个小猴子边上有 100 根香蕉，它要走过 50 米才能到家，每次它最多搬 50 根香蕉，（多了就拿不动了），它每走 1 米就要吃掉一根，请问它最多能把多少根香蕉搬到家里。*

**（提示：他可以把香蕉放下往返走，但是必须保证它每走一米都能有香蕉吃。也可以走到 n 米时，放下一些香蕉，拿着 n 根香蕉走回去重新搬 50 根。）**  
  
### 关键逻辑：  

#### 1 . 分段式搬运：  
- 猴子可以分段逐步把香蕉搬到更远的地方，每次携带部分香蕉前进，将其存放在某个距离处，然后返回拿更多的香蕉，直到最后一批香蕉被搬到目的地。  
  
#### 2 . 权衡返回和最大搬运数量：  
- 由于猴子每走一米会消耗1根香蕉，所以需要考虑，返回去消耗香蕉的代价和最终能带出来的最多数量。  
  
### 步骤解析：  
- 1 . 开局条件：  

  - 猴子有 100 根香蕉；  
  - 每次最多搬 50 根香蕉；  
  - 要求搬到家的最多香蕉数量。  
- 2 . 第一阶段：（0～17米）  
  - 猴子先搬 50 根走 17 米，放下 16 根，带 17 根回去；  
  - 再搬 50 根走 17 米，到 17 米处时剩 33 根；  
  - 一共有49根香蕉；  
- 3 . 第二阶段：（17～50米）  
  - 目前开始返回时有49根香蕉，走了33米吃掉33根香蕉，还剩下16根香蕉带回。  
- 最终结果：  猴子最多能带16根香蕉回家。  
  
## 1. 西邮Linux欢迎你啊
*请解释以下代码的运行结果。*  
```c  
int main() {
    unsigned int a = 2024;
    for (; a >= 0; a--)
        printf("%d\n", printf("Hi guys! Join Linux - 2%d", printf("")));
    return 0;
}  
```   
  
### 代码分析：    
  
- 1 . for循环：  
  - a的初始值为2024且a为无符号整数类型,判断大于0,进入循环并其后置--，直到跳出循环；
- 2 . printf库函数：    

  - 其返回值为其所打印有效字符个数；  
  - 从最内层开始，printf的返回值为0,且不打印字符；  
  - 第二层的打印为："Hi guys! Join Linux - 20"，返回值为25；  
  - 第三层的打印为："Hi guys! Join Linux - 2025"；  
   
### 最终结果：  
- for循环会一直进行下去，无限循环打印："Hi guys! Join Linux - 2025"。  
  
## 2. 眼见不一定为实
*输出为什么和想象中不太一样？*

*你了解 sizeof() 和 strlen() 吗？他们的区别是什么？*
```c    
int main() {
    char p0[] = "I love Linux";
    const char *p1 = "I love Linux\0Group";
    char p2[] = "I love Linux\0";
    printf("%d\n%d\n", strcmp(p0, p1), strcmp(p0, p2));
    printf("%d\n%d\n", sizeof(p0) == sizeof(p1), strlen(p0) == strlen(p1));
    return 0;
}  
```  
### 代码分析：  
 - 1 .  sizeof()与strlen()的比较：

    - (1) . sizeof()是关键字，用于计算变量或类型所占字节个数大小；  
    - (2) . sizeof()计算的是整个字符串所占字节的大小（包括“\0”）；
    - (3) . strlen()是函数，用于计算有效字符的个数，计算到“\0”处停止（不包括“\0”）。  
- 2 . strcmp(str1,str2)函数：
  - (1) . 对比字符串：(返回值）若str1=str2,则返回值为 0 ；
  - (2) . 若 str1 的 ascll 值大于 str2 ,返回值为正数，反之返回值为负数；  
  - (3) . 若识别到字符串中有终止字符\0则停止识别。  
- 3 . 具体步骤分析：  
  - 1 . 先对 p0 , p1 进行比较，完全相等，返回值为 0 ；  
  - 2 . 其次对 p0 , p2 进行比较，完全相等，返回值为 0 ；  
  - 3 . 由于p1为字符串指针，所以sizeof（p1）为 4 或 8 ,所以与sizeof(p0)不相等，判断结果为假，因此返回值为0；  
  - 4 . 最后 strlen(p1) == strlen(p0) ,得出返回值为 1 ；
### 得出结果：  
       0  
       0  
       0   
       1
  
    
## 3. 1.1 - 1.0 != 0.10
*为什么会这样，除了下面给出的一种方法，还有什么方法可以避免这个问题？*  
```c  
int main() {
    float a = 1.0, b = 1.1, ex = 0.1;
    printf("b - a == ex is %s\n", (b - a == ex) ? "true" : "false");
    int A = a * 10, B = b * 10, EX = ex * 10;
    printf("B - A == EX is %s\n", (B - A == EX) ? "true" : "false");
}  
```  
  
### 代码分析 ：    
- 1 . **float**：   

  - 定义浮点型的关键字,可以定义一个浮点型变量，由于计算机对数据的保存以二进制为标准，所以对于有些浮点型的数无法精确保存，例如：0.3在内存中保存为0.01001100110011...这样的无限循环小数。    

-   2 . **三目操作符（ ？ ： ）：**    

    - 运行原理为：如果前置的条件为真，则执行“？”后的语句，若为假，则执行“：”后的语句。   

- 3 . 步骤分析：  
  - 第一个由于浮点型保存并不精确，所以三目操作符中，判断语句为假执行“flase”；  
  - 第二个由于以整数保存，所以判断语句为真，执行“true”；    
### 最终结果：  
      b - a == ex is false
      B - A == EX is true
### 解决方法：  
  -   使用误差容限：  
      - 使用一个小的误差范围（epsilon）来判断两个浮点数是否“足够接近”。  
    ```c  
    #include <math.h>
    #include <stdio.h>

    int main() {
    float a = 1.0, b = 1.1, ex = 0.1;
    float diff = b - a;
    float tolerance = 1e-6;
    
    if (fabs(diff - ex) < tolerance) {
        printf("Equal within tolerance\n");
    } else {
        printf("Not equal\n");
    }
    return 0;
    }  
    ```  
  ## 4. 听说爱用位运算的人技术都不太差
*解释函数的原理，并分析使用位运算求平均值的优缺点。*  
```c  
int average(int nums[], int start, int end) {
    if (start == end)
        return nums[start];
    int mid = (start + end) / 2;
    int leftAvg = average(nums, start, mid);
    int rightAvg = average(nums, mid + 1, end);
    return (leftAvg & rightAvg) + ((leftAvg ^ rightAvg) >> 1);
}  
```  
### 代码分析：  
  - 函数：  
    - average 为一个计算平均值的函数，他的接受值为一个整形数组，两个整形变量；  
    - 首先if语句判断，同时作为一个函数递归的基准；
    - mid = (start + end) / 2，用于计算当前数组的数据索引；  
    - 函数递归：通过递归计算左半部分和右半部分的平均值；  
    - 最后左右两部分相加计算出平均值；  
  - 位运算符：  
    - leftAvg & rightAvg ：计算两个数的按位与，得出它们的共同部分。  
    - (leftAvg ^ rightAvg) >> 1，先进行按位异或，找出两个数字的的不同位，在进行右移 1 相当于除以2计算平均值；  
### 位运算符优缺点分析：  
  - 优点：    

    - 1 . 避免整数溢出；    

    - 2 . 效率较高：
      - 位运算在硬件级别非常快；

      - 避免了除法和模运算；

      - 分治结构适合并行化处理；
    - 3 . 没有浮点精度损失；  
      
- 缺点：  
     - 1 . 结果向下取整；  
     - 2 . 由于递归可能造成栈溢出；
     - 3 . 只适用于整数数组；
      
## 5. 全局还是局部!!!
*先思考输出是什么，再动动小手运行下代码，看跟自己想得结果一样不一样 >-<*  
```c  
int i = 1;
static int j = 15;
int func() {
    int i = 10;
    if (i > 5) i++;
    printf("i = %d, j = %d\n", i, j);
    return i % j;
}
int main() {
    int a = func();
    printf("a = %d\n", a);
    printf("i = %d, j = %d\n", i, j);
    return 0;
}  
```  
### 代码分析：  
  - **static：**    

    - 作用是改变变量/函数的生命周期、作用域或链接属性；  

    - 修饰局部变量：在函数第一次调用时初始化，之后保持其值，生命周期持续到程序结束。将其的存储位置从栈区改变为静态区；    

    - 修饰全局变量：将变量的外部链接属性改为“内部链接”，作用域从整个程序缩小到当前文件。    
- func函数：  
  - 由于i为局部变量定义为10,系统会先执行局部变量，所以开始的全局变量i=1不会出现；   

  - if语句判断，i=10>5，结果为真，再执行后置++;  
  - 最后返回 i % j ；  
- 主函数：  
  - 先调用func函数，用a来承接其返回值return i % j → 11 % 15 = 11；   

  - 最后进行打印：printf("i = %d, j = %d\n", i, j)，这里的 i 是全局变量 i（值为 1），j 是全局变量 j（值为 15），输出：i = 1, j = 15；  
     
### 最后结果：  
       i = 11, j = 15
       a = 11
       i = 1, j = 15


## 6. 指针的修罗场：改还是不改，这是个问题
*指出以下代码中存在的问题，并帮粗心的学长改正问题。*  
```c  
int main(int argc, char **argv) {
    int a = 1, b = 2;
    const int *p = &a;
    int * const q = &b;
    *p = 3, q = &a;
    const int * const r = &a;
    *r = 4, r = &b;
    return 0;
}  
```  
### 代码分析：  
- **const**：    

  - 用于声明常量的关键字，简单来说，const 是创建常量的一个工具。   

  - 特点：  
    - 必须初始化：声明时必须同时赋值。

    - 不可重新赋值：声明后，不能再给这个标识符赋予一个新的值。  

- 具体步骤：  

  - 第一个const修饰int类型变量，使其无法改变a的数值，但是可以改变指针p所指方向，所以不可以给*p赋值为3；  
  - 第二个const修饰p指针，以达到可以改变数值但不可以改变指向对象，所以不可以改为指向b；  
  - 第三个const修饰达到既不可以改变指向对象，又不可以改变数值大小，所以不可以更改r的值；  
  
### 修改结果：  
```c  

int main(int argc, char **argv) {
    int a = 1, b = 2;
    const int *p = &a;
    int * const q = &b;
    *p = &a, q = 3;
    const int * const r = &a;
    return 0;
}
```  
  
## 7. 物极必反？
*你了解 argc 和 argv 吗，这个程序里的 argc 和 argv 是什么？*

*程序输出是什么？解释一下为什么。*  
```c  
int main(int argc, char *argv[]) {
    while (argc++ > 0);
    int a = 1, b = argc, c = 0;
    if (--a || b++ && c--)
        for (int i = 0; argv[i] != NULL; i++)
            printf("argv[%d] = %s\n", i, argv[i]);
    printf("a = %d, b = %d, c = %d\n", a, b, c);
    return 0;
}  
```  
  
### argc 和 argv：  
- 1 .首先**argc**和 **argv**。它们是 C语言 程序中 main 函数的参数，用于接收命令行参数。  

- 2 .**argc**的类型为整数，表示命令行参数的数量，在未初始化时通常被赋值为1，而**argv**是字符指针数组，存储每个命令行参数的字符串，其中**argv**[0]为字符串首元素，往往表示该程序的文件名，**argv**[1] 到 **argv**[**argc-1**] 是我们输入的参数。  
- 3 .命令行：   
  - 是一种通过文本命令与计算机操作系统交互的界面。我们通过输入特定的文本指令来执行操作。    
    
### 代码分析：    

- 1 . **argc** 开始时为 1 ,然后进入 **while** 中不断循环，直到超出 **int** 类型范围，为  -2147483648 时跳出循环并进行打印，得出 b=-2147483648 ；  
- 2 . a前置--,所以先进行--，在判断，得出a=0；而c先进行判断再--，二者均判断为0,因此无法进入if语句；  
   
### 最终结果：  
        a = 0,b=-2147483648,c=-1  
  
## 8. 指针？数组？数组指针？指针数组？
*在主函数中定义如下变量：*  
```c  
int main() {
    int a[2] = {4, 8};
    int(*b)[2] = &a;
    int *c[2] = {a, a + 1};
    return 0;
}  
```  
*说说这些输出分别是什么？*  
```c  
a, a + 1, &a, &a + 1, *(a + 1), sizeof(a), sizeof(&a)
*b, *b + 1, b, b + 1, *(*b + 1), sizeof(*b), sizeof(b)
c, c + 1, &c, &c + 1, **(c + 1), sizeof(c), sizeof(&c)  
```  
### 代码分析：  
  - b被定义为一个指向a数组的数组指针；定义c[2]为一个里面都是指针的指针数组；  
### 结果得出：  
  - **a：**  
    - a是数组的首地址 打印出来的是第一个元素 4 的地址；  
    - a+1指向数组的第二个元素，打印出来的是第二个元素的地址；  
    - &a是整个数组的地址，数值与数组的首地址相同；  
    - &a+1为整个数组的地址+8/+16；  
    - a+1指向第二个元素，所以*(a+1) 等于 8；  
    - sizeof(&a)计算的数值为指针，所以为4/8；  
    - sizeof(a)计算为整个数组大小，所以大小为8；  
  - **b：** 
    - *b=a,所以为数值为a地址;  
    - 同理：*b + 1结果为a第二个元素的地址；  
    - 以下结果均与a组的结果相等；  
  - **c**：  
    - c是指针数组c的首地址，是指针a的地址；  
    - c+1为数组里第二个元素的地址；  
    - &c为整个数组大小，数值与首元素地址相同；  
    - &c+1为第二个元素；  
    - sizeof（c)为8/16；  
    - sizeof（&c为4/8；  
    
## 9. 嘻嘻哈哈，好玩好玩
 *在宏的魔法下，数字与文字交织，猜猜结果是什么？*   
```c  
#define SQUARE(x) x *x
#define MAX(a, b) (a > b) ? a : b;
#define PRINT(x) printf("嘻嘻，结果你猜对了吗，包%d滴\n", x);
#define CONCAT(a, b) a##b

int main() {
    int CONCAT(x, 1) = 5;
    int CONCAT(y, 2) = 3;
    int max = MAX(SQUARE(x1 + 1), SQUARE(y2))
    PRINT(max)
    return 0;
}  
```  
   
### 代码分析： 
  - **define**宏定义：   
    - 第一个宏定义：将SQUARE(x)替换为x *x；  
    - 第二个宏定义：将MAX(a, b)替换为(a > b) ? a : b;  
    - 第三个宏定义：将PRINT(x)替换为printf("嘻嘻，结果你猜对了吗，包%d滴\n", x)；  
    - 第四个宏定义：CONCAT(a, b)替换为a##b(表示将a与b连起来)；  
### 最终结果：  
  - int max = MAX(SQUARE(x1 + 1), SQUARE(y2))得出：(x1+1*x1+1,y2\*y2)；  
  - 打印出：嘻嘻，结果你猜对了吗，包11滴；  
    
## 10. 我写的排序最快
*写一个 your_sort 函数，要求不能改动 main 函数里的代码，对 arr1 和 arr2 两个数组进行升序排序并剔除相同元素，最后将排序结果放入 result 结构体中。*  
```c  

int main() {
    int arr1[] = {2, 3, 1, 3, 2, 4, 6, 7, 9, 2, 10};
    int arr2[] = {2, 1, 4, 3, 9, 6, 8};
    int len1 = sizeof(arr1) / sizeof(arr1[0]);
    int len2 = sizeof(arr2) / sizeof(arr2[0]);

    result result;
    your_sort(arr1, len1, arr2, len2, &result);
    for (int i = 0; i < result.len; i++) {
        printf("%d ", result.arr[i]);
    }
    free(result.arr);
    return 0;
}  
```  

### 代码结果：  
    #include <stdio.h>
    #include <stdlib.h>

    // 定义result结构体
    typedef struct {
    int *arr;
    int len;
    } result;  

    //冒泡排序
    void bubble_sort(int arr[], int n) {
    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                int temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
    }
    // 主要的排序去重函数
    void your_sort(int arr1[], int len1, int arr2[], int len2, result *res) {
    // 合并两个数组
    int total_len = len1 + len2;
    int *merged = (int *)malloc(total_len * sizeof(int));
    for (int i = 0; i < len1; i++) {
        merged[i] = arr1[i];
    }
    for (int i = 0; i < len2; i++) {
        merged[len1 + i] = arr2[i];
    }
    // 对合并后的数组进行排序
    quick_sort(merged, 0, total_len - 1);
    // 去重
    if (total_len == 0) {
        res->arr = NULL;
        res->len = 0;
        free(merged);
        return;
    }
    int *unique = (int *)malloc(total_len * sizeof(int));
    int unique_len = 1;
    unique[0] = merged[0];
    
    for (int i = 1; i < total_len; i++) {
        if (merged[i] != merged[i - 1]) {
            unique[unique_len++] = merged[i];
        }
    }
    res->arr = unique;
    res->len = unique_len;
    // 释放临时数组
    free(merged);
    }
    // 主函数
    int main() {
        int arr1[] = {2, 3, 1, 3, 2, 4, 6, 7, 9, 2, 10};
        int arr2[] = {2, 1, 4, 3, 9, 6, 8};
        int len1 = sizeof(arr1) / sizeof(arr1[0]);
        int len2 = sizeof(arr2) / sizeof(arr2[0]);
    result result;
    your_sort(arr1, len1, arr2, len2, &result);
    for (int i = 0; i < result.len; i++) {
        printf("%d ", result.arr[i]);
    }
    free(result.arr);
       return 0;
     }

  
## 11. 猜猜我是谁
*在指针的迷宫中，五个数字化身为神秘的符号，等待被逐一揭示。*  
``` c  
int main() {
    void *a[] = {(void *)1, (void *)2, (void *)3, (void *)4, (void *)5};
    printf("%d\n", *((char *)a + 1));
    printf("%d\n", *(int *)(char *)a + 1);
    printf("%d\n", *((int *)a + 2));
    printf("%lld\n", *((long long *)a + 3));
    printf("%d\n", *((short *)a + 4));
    return 0;
}  
```    
### 代码分析：  
  - void*：无类型指针 不可直接解引用，需要类型转换后才可以解引用；  
  - 大小端序：    

    - 大端序 ：高位字节存储在低地址，类似于我们书写数字的顺序（从左到右是高位到低位）

    - 小端序 ：低位字节存储在低地址，类似于反着书写数字。
  - 主函数分析：  
    - 1 . printf("%d\n", *((char *)a + 1))：    

      - 先将指针类型转化为char*类型，然后进行+1,由于在16进制中，每两位为一字节，所以在64位环境下可以得到指向第二个字节，为：0000,得出结果为0；  
    - 2 . printf("%d\n", *(int *)(char *)a + 1)：  
      - 先将指针转化为int类型，然后解引用后+1,得出结果为2；  
    - 3 . printf("%d\n", *((int *)a + 2))：  
      - 先将数组首地址转为 int*，然后跳过 2 个 int（8字节），指向 a[1] 的开始的4个字节 得出结果为2；  
    - 4,5与3同理，可得：4,2；  
### 最终得出：  
        0
        2
        2 
        4
        2  
  
      
## 12. 结构体变小写奇遇记
*计算出 Node 结构体的大小，并解释以下代码的运行结果。*  
``` c  
union data {
    int a;
    double b;
    short c;
};
typedef struct node {
    long long a;
    union data b;
    void (*change)( struct node *n);
    char string[0];
} Node;
void func(Node *node) {
    for (size_t i = 0; node->string[i] != '\0'; i++)
        node->string[i] = tolower(node->string[i]);
}

int main() {
    const char *s = "WELCOME TO XIYOULINUX_GROUP!";
    Node *P = (Node *)malloc(sizeof(Node) + (strlen(s) + 1) * sizeof(char));
    strcpy(P->string, s);
    P->change = func;
    P->change(P);
    printf("%s\n", P->string);
    return 0;
}  
```    
###  **联合体与结构体：**   

  - 1 . 联合体所有成员共享同一块内存空间，而结构体每个成员有独立的内存空间；  
  - 2 . 联合体的大小等于其最大成员的大小，而结构体为每个成员所占内存之和；  
### **内存对齐：**    

  - 每个成员的偏移量必须是其对齐大小的整数倍；

  - 结构体总大小必须是最大成员对齐大小的整数倍；

  - 嵌套结构体：以其最大成员的对齐要求为准；     
### 代码分析：  
  - func函数接收Node类型的字符串指针，然后进行大小写转变；   

  - 主函数：  
    - 定义字符串指针s，指向"WELCOME TO XIYOULINUX_GROUP!"；
    - 动态分配内存，将堆区内存分配给柔性字符串，使其改为字符串"WELCOME TO XIYOULINUX_GROUP!"；  
    - 调用函数func将大写转变为小写；  
    -  最后进行打印；  
### 最终结果：  
      
        sizeof(Node) = 24

        程序输出：welcome to xiyoulinux_group!

  




       




















