# 西邮 Linux 兴趣小组 2023 纳新面试题
*学长寄语：长期以来，西邮Linux兴趣小组的面试题以难度之高名扬西邮校内。我们作为出题人也清楚的知道这份试题略有难度。请你动手敲一下代码。别担心，若有同学能完成一半的题目，就已经十分优秀。其次，相比于题目的答案，我们对你的思路和过程更感兴趣，或许你的答案略有瑕疵，但你正确的思路和对知识的理解足以为你赢得绝大多数的分数。最后，做题的过程也是学习和成长的过程，相信本试题对你更加熟悉的掌握C语言的一定有所帮助。祝你好运。我们东区逸夫楼FZ103见！*

本题目只作为西邮Linux兴趣小组2023纳新面试的有限参考。  
为节省版面，本试题的程序源码省去了#include指令。  
本试题中的程序源码仅用于考察C语言基础，不应当作为C语言「代码风格」的范例。  
所有题目编译并运行于x86_64 GNU/Linux环境。  
 
## 0. 鼠鼠我啊，要被祸害了
*有1000瓶水，其中有一瓶有毒，小白鼠只要尝一点带毒的水，24小时后就会准时死亡。至少要多少只小白鼠才能在24小时内鉴别出哪瓶水有毒？*  
  
### 关键逻辑：  
  - 用二进制表示老鼠的状态，活为0，死为1；  
  - 将若干老鼠分为每一个二进制位，让对应的老鼠喝下相应的水，便可表示出该水的编号；  
    
### 具体步骤：  
  - 为了找出需要多少位数可以表示1000，所以2<sup>n</sup>>=1000,得出 n=10；
  - 让每一个老鼠代表一位，然后将瓶子编号。例如：瓶子1表示为0000000001,这里1表示1号老鼠喝，0则表示对应位置老鼠不喝；  
  - 最后死去的老鼠表示1,就可以得出该瓶子的编号；    
### 最终答案：  
        至少需要10只老鼠。  
  
## 1. 先预测一下~
*按照函数要求输入自己的姓名试试~*

```c
char *welcome() {
    // 请你返回自己的姓名
}
int main(void) {
    char *a = welcome();
    printf("Hi, 我相信 %s 可以面试成功!\n", a);
    return 0;
}
```  
### 代码分析：   
  - welcome函数：  
    - 接收值为无，返回值为char*类型；    
    - 直接用return“名字”；
 - 主函数： 
   - 先让a承接welcome的返回值，将函数中的字符串赋值给a；  
   - 然后进行打印；  

### 最终结果：  
       Hi, 我相信 自己的姓名 可以面试成功!    
  
## 2. 欢迎来到Linux兴趣小组
*有趣的输出，为什么会这样子呢~*  
```  
int main(void) {
    char *ptr0 = "Welcome to Xiyou Linux!";
    char ptr1[] = "Welcome to Xiyou Linux!";
    if (*ptr0 == *ptr1) {
      printf("%d\n", printf("Hello, Linux Group - 2%d", printf("")));
    }
    int diff = ptr0 - ptr1;
    printf("Pointer Difference: %d\n", diff);
}  
```  
### 代码分析：  
  - 1 . printf库函数：    

    - 其返回值为其所打印有效字符个数；    
      
  - 2 . 变量定义和内存布局：  
    
     - ptr0 是指针，指向字符串常量（在只读数据段）；
     - ptr1 是数组，在栈上开辟空间并拷贝字符串内容；   

  - 3 . 主函数：  
   - *ptr0为字符串上的首位元素，为w，\*ptr1为数组上第一个元素，所以为w，二者相等if判断成立，得出打印 Hello, Linux Group - 2023；  
   - diff = ptr0 - ptr1：  
     - 这是指针之间的运算：若ptr0在更高地址，则diff结果为正数，反之则为负数 (高低地址分布看编译器) ；
### 最终结果：  
      Hello, Linux Group - 2023
      Pointer Difference: 1431669140

## 3. 一切都翻倍了吗
*1.请尝试解释一下程序的输出。*  
*2.请谈谈对sizeof()和strlen()的理解吧。*  
*3.什么是sprintf()，它的参数以及返回值又是什么呢？*  
```  c
int main(void) {
    char arr[] = {'L', 'i', 'n', 'u', 'x', '\0', '!'}, str[20];
    short num = 520;
    int num2 = 1314;
    printf("%zu\t%zu\t%zu\n", sizeof(*&arr), sizeof(arr + 0),
           sizeof(num = num2 + 4));
    printf("%d\n", sprintf(str, "0x%x", num) == num);
    printf("%zu\t%zu\n", strlen(&str[0] + 1), strlen(arr + 0));
}  
```  
  
### sizeof()与strlen()的比较：

- (1) . sizeof()是关键字，用于计算变量或类型所占字节个数大小；  
- (2) . sizeof()计算的是整个字符串所占字节的大小（包括“\0”）；
- (3) . strlen()是函数，用于计算有效字符的个数，计算到“\0”处停止（不包括“\0”）。   

 ### sprintf()库函数：  
   - sprintf 是 C 语言标准库中的一个函数，用于将格式化的数据写入字符串（字符数组），其功能与 printf 类似，但 printf 是输出到标准输出（如屏幕），而 sprintf 是输出到指定的字符串缓冲区。    

   - 返回值：成功时返回写入字符串的字符数（不包括结尾的空字符 '\0'）；失败时返回负数（通常为 -1）。    
     
### 代码分析：  
  - 第一个printf：  
    - sizeof(*&arr)意为&arr解地址，为整个字符串，答案为7；  
    - sizeof(arr+0)表示为字符串首元素地址，类型为指针，大小为8；  
    - sizeof(num = num2 + 4),类型为short字节大小为2；  
  
  - 第二个printf：  
    - 由于sprintf返回值为字符串个数，所以答案为4；  
    - 4==520,为假返回0；  
  - 第三个printf：  
    - strlen(&str[0] + 1)：&str[0] + 1 指向 str 的第二个字符（即 "x208" 的首地址），字符串长度为 3  
    - strlen(arr + 0)：arr + 0 指向 arr 首元素，strlen 遇 '\0' 停止计数，arr 中 '\0' 前有 5 个字符，结果为 5。  
### 最终结果：  
      7       8       2
      0
      3       5
  
## 4. 奇怪的输出
*程序的输出结果是什么？解释一下为什么出现该结果吧~*  
```c  
int main(void) {
    char a = 64 & 127;
    char b = 64 ^ 127;
    char c = -64 >> 6;
    char ch = a + b - c;
    printf("a = %d b = %d c = %d\n", a, b, c);
    printf("ch = %d\n", ch);
}  
```  
  
### 代码分析：  
  - a为64与127的按位或值，为 64；
  - b为64与127的按位异或值，为63；  
  - c为-64右移6位，为-1；  
  - ch为64+63+1=128；  
    
### 最终结果：  
       a = 64 b = 63 c = -1
       ch = 128
  
## 5. 乍一看就不想看的函数
*“人们常说互联网凛冬已至，要提高自己的竞争力，可我怎么卷都卷不过别人，只好用一些奇技淫巧让我的代码变得高深莫测。”*

*这个==func()==函数的功能是什么？是如何实现的？*  
  
```c  
int func(int a, int b) {
    if (!a) return b;
    return func((a & b) << 1, a ^ b);
}
int main(void) {
    int a = 4, b = 9, c = -7;
    printf("%d\n", func(a, func(b, c)));
}  
```  
  
### 代码分析：  
  - 1 . func() 函数的功能：  
    - func(a, b) 的功能是计算两个整数 a 和 b 的和，即实现 a + b 的效果。它通过位运算模拟加法过程，不依赖 + 等算术运算符。  
  - 2 . 原理：  
    - 无进位相加：用异或运算（a ^ b） 实现；  
    - 进位计算：用与运算 + 左移 1 位（(a & b) << 1） 实现；    
    - func() 通过位运算递归模拟加法的进位和无进位过程，实现了两个整数的求和；
### 最终结果：  
      6  
   
## 6. 自定义过滤
*请实现filter()函数：过滤满足条件的数组元素。*

*提示：使用函数指针作为函数参数并且你需要为新数组分配空间。*  
```c  
typedef int (*Predicate)(int);
int *filter(int *array, int length, Predicate predicate,
            int *resultLength); /*补全函数*/
int isPositive(int num) { return num > 0; }
int main(void) {
    int array[] = {-3, -2, -1, 0, 1, 2, 3, 4, 5, 6};
    int length = sizeof(array) / sizeof(array[0]);
    int resultLength;
    int *filteredNumbers = filter(array, length, isPositive,
                                  &resultLength);
    for (int i = 0; i < resultLength; i++) {
      printf("%d ", filteredNumbers[i]);
    }
    printf("\n");
    free(filteredNumbers);
    return 0;
}  
```  
  
### 函数补全：  
```  c
int *filter(int *array, int length, Predicate predicate, int *resultLength) {
    首先计算满足条件的元素个数
    int count = 0;
    for (int i = 0; i < length; i++) {
        if (predicate(array[i])) {
            count++;
        }
    }
    
    为新数组分配空间   

    int *result = (int *)malloc(count * sizeof(int));    

    检验分配结果  

    if (result == NULL) {
        *resultLength = 0;
        return NULL;
    }
    
    填充满足条件的元素到新数组  

    int index = 0;
    for (int i = 0; i < length; i++) {
        if (predicate(array[i])) {
            result[index++] = array[i];
        }
    }
    
    *resultLength = count;
    return result;
}
```  
### 整体思路：  
  - 首先计算所需空间大小；  
  - 然后用malloc进行动态内存分配 ；  
  - 最后将数组填入新空间；  
  
## 7. 静…态…
*1.如何理解关键字static？*  
*2.static与变量结合后有什么作用？*  
*3.static与函数结合后有什么作用？*  
*4.static与指针结合后有什么作用？*   
*5.static如何影响内存分配？*  
  
### - **static：**    

  - 作用是改变变量/函数的生命周期、作用域或链接属性；  

  - 修饰局部变量：在函数第一次调用时初始化，之后保持其值，生命周期持续到程序结束。将其的存储位置从栈区改变为静态区；    

  - 修饰全局变量：将变量的外部链接属性改为“内部链接”，作用域从整个程序缩小到当前文件；  
  - static 与函数结合：用来限制作用域，使其只在定义它的源文件内可见；  
  - static 与指针结合：可以创建持久的指针变量或返回静态数据的指针；  
  - static 对内存影响：将变量从栈移到数据段(静态区），程序运行期间始终存在；  
  
## 8. 救命！指针！
*数组指针是什么？指针数组是什么？函数指针呢？用自己的话说出来更好哦，下面数据类型的含义都是什么呢？*  
```c   
int (*p)[10];
const int* p[10];
int (*f1(int))(int*, int);  
```  
  
### 指针与数组：  
  - 指针数组为数组，是里面元素均为指针的数组；  
  - 数组指针为指针，是一个指向数组的指针；  
  - 函数指针为指向函数的指针；  
  
### 具体分析：  
  -  (*p)[10]为数组指针，指向一个包含10个元素的数组；  
  - * p[10]为指针数组，一个包含10个指针元素的数组；  
  - f1(int)为以后一个函数，接收一个整形返回一个指针的函数，使其达到一个函数指针的作用，然后解引用达到调用函数的作用；  
    
## 9. 咋不循环了
*程序直接运行，输出的内容是什么意思？*  
```c  
int main(int argc, char* argv[]) {
    printf("[%d]\n", argc);
    while (argc) {
      ++argc;
    }
    int i = -1, j = argc, k = 1;
    i++ && j++ || k++;
    printf("i = %d, j = %d, k = %d\n", i, j, k);
    return EXIT_SUCCESS;
}  
```  
### argc 和 argv：  
- 1 .首先**argc**和 **argv**。它们是 C语言 程序中 main 函数的参数，用于接收命令行参数。  

- 2 .**argc**的类型为整数，表示命令行参数的数量，在未初始化时通常被赋值为1，而**argv**是字符指针数组，存储每个命令行参数的字符串，其中**argv**[0]为字符串首元素，往往表示该程序的文件名，**argv**[1] 到 **argv**[**argc-1**] 是我们输入的参数。  
- 3 .命令行：   
  - 是一种通过文本命令与计算机操作系统交互的界面。我们通过输入特定的文本指令来执行操作。    
  
### 代码分析：  
  - while循环：  
    - 由于argc初始值为1,所以判断后不断循环，直到达到整数溢出后成为负数，继续++循环，最终为0时跳出循环；    

  - 此时i=-1,j=0,k=1,进行i++ && j++ || k++运算后，得出i=0 , j=1 , k=2;  
### 最终结果：  
       i++ && j++ || k++  
  
## 10. 到底是不是TWO  
```c  
#define CAL(a) a * a * a
#define MAGIC_CAL(a, b) CAL(a) + CAL(b)
int main(void) {
  int nums = 1;
  if(16 / CAL(2) == 2) {
    printf("I'm TWO(ﾉ>ω<)ﾉ\n");
  } else {
    int nums = MAGIC_CAL(++nums, 2);
  }
  printf("%d\n", nums);
}  
```   
  
### 代码分析：  
  - define宏定义：  
    - 第一个宏定义：将CAL(a)替换为a*a\*a；  
    - 第二个宏定义：将MAGIC_CAL(a, b)替换为CAL(a) + CAL(b)；  
  
  - 主函数：  
    - if语句中将CAL（2）替换为2*2\*2，得出32==2,为假，判断结果为0,不执行if语句；  
    - else语句中nums被赋值为44,但是由于在else中为局部变量，所以出作用域便被销毁，得出最终结果为nums=1;  
### 最终答案：  
        1  
  
## 11. 克隆困境
*试着运行一下程序，为什么会出现这样的结果？*

*直接将s2赋值给s1会出现哪些问题，应该如何解决？请写出相应代码。*  
  
```c  
struct Student {
    char *name;
    int age;
};

void initializeStudent(struct Student *student, const char *name,
                       int age) {
    student->name = (char *)malloc(strlen(name) + 1);
    strcpy(student->name, name);
    student->age = age;
}
int main(void) {
    struct Student s1, s2;
    initializeStudent(&s1, "Tom", 18);
    initializeStudent(&s2, "Jerry", 28);
    s1 = s2;
    printf("s1的姓名: %s 年龄: %d\n", s1.name, s1.age);
    printf("s2的姓名: %s 年龄: %d\n", s2.name, s2.age);
    free(s1.name);
    free(s2.name);
    return 0;
}  
```  
### 代码分析：   
  
  - 若直接运行则会出现：s1的姓名和年龄都和和s2相同；  
    
#### 问题：  
  - 由于s2直接赋值给s1，导致s1中的”tom“等数据失效，使其无法进行内存释放，导致内存泄漏；   
  - 这之后由于s1与s2指向同一块内存，主函数中两次释放，重复释放(悬空指针释放）可能导致数据损坏等较为严重的内存问题；  
    
### 解决方案：   
```c
struct Student {
    char name[100];
    int age;
};

void initializeStudent(struct Student *student, const char *name,
                       int age) {
    strcpy(student->name, name);
    student->age = age;
}
int main(void) {
    struct Student s1, s2;
    initializeStudent(&s1, "Tom", 18);
    initializeStudent(&s2, "Jerry", 28);
    s1 = s2;
    printf("s1的姓名: %s 年龄: %d\n", s1.name, s1.age);
    printf("s2的姓名: %s 年龄: %d\n", s2.name, s2.age);
    return 0;
}    
```  
  - 不用malloc动态分配内存，而使用固定数组大小来存放字符串，这样就避免了直接赋值时会使指针失效的问题了；  
    
## 12. 你好，我是内存
*作为一名合格的C-Coder，一定对内存很敏感吧~来尝试理解这个程序吧！*  
```  c
struct structure {
    int foo;
    union {
      int integer;
      char string[11];
      void *pointer;
    } node;
    short bar;
    long long baz;
    int array[7];
};
int main(void) {
    int arr[] = {0x590ff23c, 0x2fbc5a4d, 0x636c6557, 0x20656d6f,
                 0x58206f74, 0x20545055, 0x6577202c, 0x6d6f636c,
                 0x6f742065, 0x79695820, 0x4c20756f, 0x78756e69,
                 0x6f724720, 0x5b207075, 0x33323032, 0x7825005d,
                 0x636c6557, 0x64fd6d1d};
    printf("%s\n", ((struct structure *)arr)->node.string);
}
```  
    
### - 大小端序：    

  - 大端序 ：高位字节存储在低地址，类似于我们书写数字的顺序（从左到右是高位到低位）

  - 小端序 ：低位字节存储在低地址，类似于反着书写数字。
### 代码分析：  
  - (struct structure *)arr)->node.string：  
    - (struct structure *)arr：是将类型为int的数组指针arr转化成struct structure类型指针；  
    -  (struct structure *)arr)->node：是指针访问结构体成员node；  
    - (struct structure *)arr)->node.string：为访问联合体node中的成员string;  
  
  - 原来string中为arr内容全部，但是由于结构体中的内存对齐性质，使得其开始访问的元素为string+8字节（也就是arr[2]）；  
  - 由于该计算机的小端序的存储方式，低位字节存储在低地址，所以访问从末尾开始：             
      
        0x636c6557 小端序内存字节：57 65 6c 63  
        0x20656d6f 小端序内存字节：6f 6d 65 20  
        0x58206f74 小端序内存字节：74 6f 20 58  
        ...  
        0x7825005d 小端序内存字节：5d 00 25 78  
      
  - 然后根据ascll码表打出字符；  
    
### 最后得出：  
  
      Welcome to XUPT , we come to Xiyou Linux Group [2023]  
        
## 13. GNU/Linux (选做)
*注：嘿！你或许对Linux命令不是很熟悉，甚至你没听说过Linux。但别担心，这是选做题，了解Linux是加分项，但不了解也不扣分哦！*

*你知道cd命令的用法与 / . ~ 这些符号的含义吗？*
*请问你还懂得哪些与 GNU/Linux 相关的知识呢~*

# 结语

**恭喜你攻克所有难关！迎难而上的决心是我们更为看重的。**  
**来到这里的人已是少数，莫踌躇在成功的门槛前。**  
**自信一点，带上你的笔记本电脑，来东区逸夫楼FZ103面试吧！**















